[project]
name = "django-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
    "whitenoise",
    "azure-monitor-opentelemetry>=1.8.9",
    "python-dotenv>=1.2.3",
    "uvicorn>=0.30.0",
]

[dependency-groups]
dev = [
    "debugpy>=1.8.0",
]
